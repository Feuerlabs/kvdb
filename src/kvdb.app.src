%% -*- erlang -*-
{application, kvdb,
 [
  {description, "key value database API"},
  {vsn, git},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
		  gproc
                 ]},
  {mod, {kvdb_app, []}},
  {start_phases, [{open_dbs, []}]},
  {env, [
	 {backend, ets}  % default
	 %% {dir, DbDirectory}
	]}
 ]}.
